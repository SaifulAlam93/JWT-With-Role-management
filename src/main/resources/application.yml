spring:
  application:
    name: nidleLicence
  jpa:
    hibernate:
      ddl-auto: update
  jmx:
    enabled: false
  jackson:
    serialization:
      WRITE_DATES_AS_TIMESTAMPS: false
jwtSecret: s3cret
jwtExpirationInSeconds: 86400 # 1 Day
jwtExpirationInMonths: 3
server:
  port: 8080

app:
  startup:
    generate-menu: true
